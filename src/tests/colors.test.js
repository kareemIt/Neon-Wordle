import {
  handleGreen,
  handleYellow,
  handleColorGreen,
  handleColorYellow,
} from '../utilities/handleColors';
describe('Color utilities', () => {
  it('changes color', () => {
    expect(false);
  });
});
